# Aurelia Jewels Frontend

Run:

npm install
npm run dev

Open http://localhost:5173

The storefront first tries `GET /api/products`; if the backend/MongoDB is unavailable, it shows built-in demo products so the UI still opens.
